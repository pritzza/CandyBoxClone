#pragma once

/*
class SpriteTexture;

class Entity
{
public:
	SpriteTexture* sprite;
	int xPos;
	int yPos;

public:
	Entity(int x, int y, SpriteTexture* s) : xPos(x), yPos(y), sprite(s) {}
	Entity(const Entity& e);
};

*/